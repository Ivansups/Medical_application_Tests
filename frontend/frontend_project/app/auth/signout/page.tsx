// Страница выхода из системы
// Здесь нужно создать страницу подтверждения выхода или автоматический выход

// Импорты, которые понадобятся:
// import { signOut } from "next-auth/react"
// import { getServerSession } from "next-auth"
// import { redirect } from "next/navigation"

export default function SignOutPage() {
  // Здесь нужно:
  // 1. Проверить, авторизован ли пользователь
  // 2. Показать страницу подтверждения выхода
  // 3. Или автоматически выйти и перенаправить
  // 4. Обработать ошибки
  
  return (
    <div>
      {/* 
        Здесь должна быть страница выхода:
        
        1. Сообщение "Вы уверены, что хотите выйти?"
        2. Кнопка "Да, выйти" (вызывает signOut())
        3. Кнопка "Отмена" (возврат на предыдущую страницу)
        4. Или автоматический выход без подтверждения
      */}
      
      <h1>Выйти из системы</h1>
      
      {/* Пример структуры:
      <div>
        <p>Вы уверены, что хотите выйти?</p>
        <button onClick={() => signOut({ callbackUrl: '/' })}>
          Да, выйти
        </button>
        <button onClick={() => window.history.back()}>
          Отмена
        </button>
      </div>
      */}
    </div>
  )
}
